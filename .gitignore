# ============================================================
# SDLC process & coordination files — NOT part of the shipped
# product. Excluded by default per DECISIONS.md D4 (see
# REQUIREMENTS/PRODUCT_OWNER_PROCESS.md § Version Control &
# Publishing). Versioning some/all of this instead is a recorded
# exception, not a silent edit — see D4.
# ============================================================
CLAUDE.md
.claude/
REQUIREMENTS/
ProductOwner_records/
/PRODUCT_OWNER_INBOX.md
**/requirements/
TASKS.md
**/history/

# Tester (QA) sub-team: validates only, never ships production code —
# entire directory is SDLC tooling (router.php is local dev infra, not a deliverable).
/tester/

# ============================================================
# SDLC package's own authoring/reference material (this repo was
# bootstrapped from the SDLC template package) — not a product
# deliverable either. (Root README.md was replaced with a project-specific
# one and is intentionally NOT excluded here.)
# ============================================================
/PRODUCT_OWNER_PROCESS.md
/roles/
/templates/

# ============================================================
# Local scratch / ad hoc dev artifacts — not part of the shipped product
# ============================================================
/archive/
/ccc.bat

# ============================================================
# Private research material & knowledge base — user's own IP,
# never published or committed. Source documents (/Dox/) and the
# derived knowledge base (/KB/) built from them.
# ============================================================
/Dox/
/KB/

# ============================================================
# Common OS / editor cruft
# ============================================================
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/
*.log
.env
.env.*

# ============================================================
# Stack-specific ignores (PHP + MySQL, per D1)
# ============================================================
vendor/
server/storage/*.sqlite
server/.phpunit.cache/

# HF-01 / CR-TEST-21: deploy-generated one-time OPcache reset token — never committed, written
# fresh by .github/workflows/deploy.yml and deleted from the live host after each deploy.
server/public/opcache-reset.token
